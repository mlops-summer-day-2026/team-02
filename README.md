# team-02 · Вайбкодинг-тимбилдинг «Строим для своих»

**14 августа 2026** · [Правила, тайминг, все команды](https://github.com/mlops-summer-day-2026)

🔗 **Демо вашей команды:** https://mlops-summer-day-2026.github.io/team-02/
Пока в репозитории нет `index.html`, по ссылке будет 404 — это нормально, она оживёт
через минуту после первого пуша.

Что строить и на чём — решает команда: сайт, телеграм-бот, что-то своё. Важно одно:
через два часа заказчик тыкает работающее демо со сцены, а зал смотрит его с телефонов.

---

## Первые 10 минут

Порядок именно такой — он экономит потом полчаса.

- [ ] **Забрали репозиторий** — все, кто будет пилотировать

  ```bash
  git clone https://github.com/mlops-summer-day-2026/team-02 && cd team-02 && git config core.hooksPath .githooks
  ```

  Хвост команды включает хук, который не даст случайно закоммитить ключ.
  Не пускает пушить — значит, не принято приглашение в организацию:
  https://github.com/orgs/mlops-summer-day-2026/invitation

- [ ] **Положили ключ** — `cp .env.example .env`, вставили ключ OpenRouter из чата капитанов

- [ ] **Заполнили [AGENTS.md](AGENTS.md)** — продукт, заказчик, его боль, стек.
      Это общий контекст, его читают все агенты команды: и Claude Code, и Codex, и Cursor.
      `CLAUDE.md` — просто ссылка на него, правьте `AGENTS.md`

- [ ] **Поделили файлы между пилотами** — главное правило дня, подробности в `AGENTS.md`.
      Делим не задачи, а файлы: один файл — один хозяин. Тогда мержить будет почти нечего

- [ ] **Задеплоили пустышку и открыли с телефона.** Любой `index.html` с одним словом.
      Убедиться, что труба работает, надо сейчас, а не за двадцать минут до защиты

---

## Ключ

Один на всех — OpenRouter, через него и текст, и картинки. Голос дешевле и надёжнее
брать браузерным `speechSynthesis`, он работает без интернета и без API.
Готовые вызовы лежат в [AGENTS.md](AGENTS.md).

Репозиторий публичный. Ключ, вписанный в JS статичной страницы, видно всем — на этом
мероприятии так можно, лимит трат стоит, но решайте это осознанно, а не молча за агентом.
В коммиты ключ не попадает никогда: `.env` в `.gitignore`, плюс хук.

---

## Демо

**Статичная страница** — самый короткий путь. `index.html` в корень, пуш, через минуту
живёт на https://mlops-summer-day-2026.github.io/team-02/ и открывается с любого телефона.
Pages уже включены, настраивать нечего.

**Телеграм-бот** — токен у @BotFather за 30 секунд, живёт пока запущен на вашем ноутбуке.
Не закрывайте крышку перед выходом на сцену.

Перед защитой проверьте: открывается с телефона по мобильному интернету, кнопки жмутся
пальцем, шрифт читается с трёх метров. И **запишите видео работающего демо** за пятнадцать
минут до выхода — это парашют на случай, если вайфай ляжет на сцене.

---

## Если сломалось

**`! [rejected] ... fetch first`** — кто-то запушил раньше: `git pull --rebase && git push`

**Не пускает пушить** — не принято приглашение в организацию, ссылка выше

**Демо 404** — `index.html` должен лежать в корне, а не в подпапке. После пуша страница
собирается около минуты, дальше это кеш браузера

**Агент снёс чужой код** — `git log --oneline`, найти живой коммит, `git revert <хеш>`.
Запушенное не пропадает никогда

**Ключ перестал работать** — скорее всего утёк в публичный код и отозван. Сразу в чат
капитанов, не гадайте
